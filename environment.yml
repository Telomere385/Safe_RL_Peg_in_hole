name: safe_rl
channels:
  - defaults
  - conda-forge
dependencies:
  - python=3.11
  - pip
  - pip:
    - torch==2.7.0
    - numpy==1.26.0
    - mushroom-rl==2.0.0rc1
    - wandb
    # IsaacSim 由 mushroom-rl 2.0 的 IsaacSim 环境类托管 (pip install isaacsim 即可)
